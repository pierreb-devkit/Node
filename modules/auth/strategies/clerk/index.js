/**
 * Clerk authentication strategy placeholder.
 *
 * This module will be implemented when Clerk integration is added.
 * For now it exports a no-op initializer so the strategy glob loader
 * does not break.
 */
export default () => {
  // No-op: Clerk strategy not yet implemented
};
